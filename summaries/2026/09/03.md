# Activity Summary for 9/3/2026

## 1:44:15 PM
The `g:\wamp64\www\sitenettech\api-client\src\pages\backend\Subscription\SubscriptionList.jsx` file, last modified on `9/3/2026, 12:48:41 PM`, defines a React component (`SubscriptionPlanList`) responsible for managing subscription plans within a backend interface.

**Key File-Specific Updates:**

*   **Core Functionality**: The component provides a comprehensive solution for displaying, creating, editing, and soft-deleting subscription plans.
*   **Data Management**: It integrates with Supabase (`supabase from '../../../../intigrations/SupabaseClient'`) for all CRUD operations on the `subscription_plans` table. Data fetching (`fetchPlans`) includes pagination, filtering (by `deleted_at`), and ordering.
*   **Form Handling and Validation**: `formik` is extensively used for both the create/edit plan form (`planForm`) and the filtering form (`filterData`). `Yup` schemas are implemented for robust client-side validation, including conditional validation for `plan_quantity` and `active_days` based on `is_lifetime`.
*   **State Management**: Numerous `useState` hooks manage UI states (loading, button loading, modal visibility, filter status), data (`tableData`, `totalCount`, `filteredData`), and selected plan information (`selectedPlanId`). `useRef` is used for PrimeReact `Toast` notifications.
*   **UI Components**: It leverages a wide array of custom and third-party UI components, including `DataTableList`, `ModalCustom`, `FilterModal`, `Pageheader`, `Button`, icons (`FaCog`, `HiOutlineRefresh`, `FaRegSquarePlus`), and Flowbite's `Tooltip`. Dedicated modal bodies (`SubscriptionPlanModalBody`, `SubscriptionFilterBody`, `ConfirmModalBody`) are imported, suggesting a structured UI.
*   **Filtering and Pagination**: `useFilterPagination` custom hook manages pagination logic. A separate `filterData` formik instance handles advanced filtering criteria, submitting via the `handleFilterAndExport` utility.
*   **Authentication and Permissions**: `useContextAuth` provides access to user authentication details (`user`, `token`, `hasPermission`), used for auditing (`created_by`, `updated_by`) and permission checks (e.g., `isSuperAdmin` is temporarily hardcoded to `true`).
*   **Soft Deletion**: Plans are soft-deleted by updating the `deleted_at` field with a timestamp and the `updated_by` user ID, rather than permanent removal. `fetchPlans` explicitly filters out soft-deleted records.
*   **Configuration**: It imports `defaultSubscriptions` and `subscriptionFilterQuery` from `../../../constants/Settings/Subscriptions`, indicating that table headers, filter configurations, and default plan values are externalized for easier management.
*   **User Feedback**: `showSuccess` and `showError` utility functions (using `primereact/toast`) provide consistent feedback to the user on operation outcomes.

**Patterns and Recurring Elements:**

*   **Supabase Interaction**: A consistent pattern of interacting with Supabase for data operations, typically involving `supabase.from('table_name').select()...` for reads and `supabase.from('table_name').insert() / .update()...` for writes.
*   **Formik + Yup for Forms**: All forms (create/edit plan, filter) follow the `useFormik` pattern with `Yup` for schema-based validation.
*   **Loading Indicators**: `setLoading(true/false)` and `setBtnLoading(true/false)` are systematically used before and after asynchronous operations to manage UI loading states.
*   **Modular Component Design**: The component heavily relies on importing smaller, specialized components (e.g., `SubscriptionPlanModalBody`, `ActionColumnTemplate`, `EmptyData`), promoting reusability and separation of concerns.
*   **Utility-Driven Logic**: A clear reliance on external utility functions (`listDataDelete`, `generateTableColumns`, `handleFilterAndExport`, `showSuccess`, `showError`, `useFilterPagination`) to abstract common logic and keep the component clean.
*   **`useEffect` Hooks for Data Lifecycle**: `useEffect` hooks are strategically placed to trigger data fetching or filtering based on changes in pagination, filter state, or authentication token.
*   **Soft Deletion Strategy**: The `deleted_at` column is consistently used across fetch and delete operations, indicating a project-wide soft deletion strategy.