# Activity Summary for 5/18/2026

## 1:49:43 PM
This log primarily details changes within two React components related to flight search functionality: `FlightSearch.jsx` and `SingleAirsegment.jsx`, both part of a "Travnet Tech CRM" project developed by Mehdi Hasan and Motalib Hossain.

### File: `g:\wamp64\www\sitenettech\api-client\src\components\Backend\Search\Flight\FlightSearch\FlightSearch.jsx`

**Key Information:**
This component is responsible for rendering flight search results. It integrates heavily with Redux for state management, handling loading states, current page, sorting options, and "load more" functionality.

**Functionalities:**
*   **State Management:** Manages `flightSearchLoading`, `searchLoader`, `currentPage`, `flightsPerPage`, `sortOption`, and `loadMoreLoading` using React's `useState` hook.
*   **Redux Integration:** Uses `useDispatch` and `useSelector` (with `shallowEqual`) to interact with `flightSearchSlice` and `flightsSlice`, fetching data such as filtered flights, loading statuses (airports, airlines), and currency.
*   **Data Fetching (`fetchResult`):** Asynchronously fetches flight data based on `searchParam` from `location.state` or query parameters. It then extracts carrier codes and airports, dispatches actions to set stops, min/max prices, fetch airport/airline details, and sets fare rules. It also stores `startTime` in `sessionStorage`.
*   **Load More (`handleLoadMore`):** Increments the `currentPage` with a simulated delay to fetch more results.
*   **Lifecycle Hook (`useEffect`):** Triggers `fetchResult` when `searchParam` or `dispatch` changes.
*   **Loader (`loader`):** Displays a `FlightLoading` component during data fetching.
*   **Fare Rules (`getInitialAvailStrings`):** A helper function to extract `availString` for each multi-flight object.
*   **Flight Selection (`selectFlightHandeller`):** An asynchronous callback that selects a flight, initializes the cart basket, adds the selected flight to the cart, stores currency and basket expiry in `sessionStorage`, and navigates to the `/admin/cart` page. Error handling is included.
*   **Sorting Options:** Defines an array `sortOptions` for various sorting criteria like "Recommended", "Price (lowest to highest/highest to lowest)", and "Duration (shortest/longest)".
*   **Duration Calculation (`calculateMultiFlightDuration`):** A helper function to calculate the total duration of a multi-flight segment.

**Timestamp Analysis (5/18/2026, 12:56:06 PM - 1:37:06 PM):**
All recorded entries for this file show virtually identical code snippets. This suggests that during this time, either no significant code changes were made to the core logic presented in these snippets, or the changes were very minor and not captured by these specific logs, or it might indicate frequent saving without substantive modifications. The component's functionality remained consistent across these timestamps.

### File: `g:\wamp64\www\sitenettech\api-client\src\components\Backend\Search\Flight\AirSegments\SingleAirsegment.jsx`

**Key Information:**
This component displays individual flight segments within the search results, providing detailed information about each flight.

**Functionalities:**
*   **Component Props:** Accepts `flight` data, `index`, `multiFlightIndex`, `isSelected`, `setSegmentValue`, `selectedValue`, and `showSelection` props.
*   **Redux Integration:** Uses `useSelector` to retrieve flight search parameters and flight data (airlines, airports).
*   **Loading State:** Displays a `Skeleton` loading indicator if the `flight` prop is not available.
*   **Flight Selection:** Conditionally renders a `RadioSimple` component for selecting a flight, setting its `availString` as the value.
*   **Flight Details Display:**
    *   Shows airline logo, flight code, and flight number.
    *   Displays baggage information (check-in and cabin baggage) with conditional rendering.
    *   Presents flight origin (departure time, airport code, date), travel time (duration, number of stops, represented visually), and destination (arrival time, airport code, date).
*   **Accordion Toggle:** Includes a `FaChevronDown` icon to toggle an accordion-like `FlightDetails` section (which is commented out in the provided code).
*   **Accessibility:** Employs `aria-label` attributes extensively on interactive elements and tooltips for improved accessibility.

**Timestamp Analysis:**
*   **5/18/2026, 1:44:57 PM:** The initial version includes `Tooltip` components around various elements like baggage info (`IoBriefcaseOutline`, `LuBaggageClaim`), airline names (`AirlineNames`), and airport names (`AirportName`), providing detailed information on hover.
*   **5/18/2026, 1:46:34 PM:** **Significant Change:** All `Tooltip` components that provided detailed information for baggage, airline names, and airport names (`AirlineNames`, `AirportName`, and baggage icons) were commented out. While the icons and data checks remain, the interactive tooltips displaying the actual descriptive content have been removed from these specific elements. The overall structure and other functionalities of the component, such as flight selection and basic display of flight times/stops, remain unchanged.

### Patterns and Recurring Elements:
*   **Consistent File Paths:** Both files reside in a structured `components/Backend/Search/Flight` directory, indicating a modular approach for the flight search feature.
*   **Redux-centric State Management:** Both components heavily rely on Redux for global state access and updates, utilizing `useSelector` and `useDispatch`.
*   **React Hooks:** Extensive use of `useState`, `useEffect`, `useMemo`, and `useCallback` for component logic and performance optimization.
*   **UI Libraries/Icons:** Imports from `react-icons/fa6` and `react-icons/ri` (for FlightSearch) and `react-icons/fa6`, `react-icons/io5`, `react-icons/lu` (for SingleAirsegment) suggest a shared icon library. `Ant Design` components (`Collapse`, `Select`) and `flowbite-react` components (`Tooltip`) are also used.
*   **Custom Utility Functions:** Common utility functions for flight search (e.g., `exportCarrierCode`, `getAirports`, `formatedCode`) are imported from a centralized `constants/allFunctions/Search/flight` path.
*   **Detailed Documentation:** Both files start with a comprehensive comment block detailing the component name, project, copyright, and developers.
*   **Future-dated Timestamps:** All timestamps are dated "5/18/2026", which might be a placeholder or an issue with the logging system.
*   **Session Storage Usage:** `FlightSearch.jsx` utilizes `sessionStorage` for temporary data like `startTime`, `cartCurrency`, and `basketExpiry`.