# Activity Summary for 3/4/2026

## 2:12:17 PM
The file `FolderReview.jsx` at `g:\wamp64\www\sitenettech\api-client\src\pages\backend\Folder\FolderReview\FolderReview.jsx` was updated on `March 4, 2026, 1:35:07 PM`.

**Key Updates:**

*   **Extensive State Management:** The component features numerous state variables using `useState` to manage UI elements (e.g., `isCollapsed`, `pageLoading`, modal visibility like `showPNRModal`, `showExistingPNRModal`), form inputs (`pnrInput`), and core data (`data`, `folderData`, `userData`, `travelOption`).
*   **Data Fetching and Processing:**
    *   It fetches user details from Supabase (`supabase.from('users')`) based on a user ID, using the `userInfo` asynchronous function.
    *   It utilizes `useFetchCompany` to retrieve company information.
    *   Initial `folderData` is processed from `location.state?.parsedData` or retrieved from `sessionStorage`. This includes building `updatedTickets` by combining passenger and air segment information, formatting dates with `dayjs`.
    *   A `getFolderOptions` function dynamically determines available travel options (e.g., air segments, tickets, hotels) based on the received folder data and a `services` constant.
*   **UI/UX Enhancements:**
    *   Implements a collapse/expand functionality with `isCollapsed` and `setShowCollapseIcon` states.
    *   Integrates `Toast` for success/error messages (`showError`, `showSuccess`).
    *   Features a `ModalCustom` component, specifically `showPNRModal`, which is displayed if `pnrInput` is empty and the modal hasn't been shown, prompting for PNR entry.
    *   Provides tabbed navigation (`tabs`, `childTabs`) to organize content, including "Passenger," "Itinerary Item," "Air Segment," and "Air Ticket."
    *   Includes a `Pageheader`, `Button` components, and an `ErrorBoundary`.
*   **Routing and Context Integration:**
    *   Uses `useNavigate`, `useLocation`, and `useParams` from `react-router-dom` to handle navigation, access state passed through routes, and extract URL parameters (`id`).
    *   Leverages `useContextAuth` to access authentication status, user information, and permissions.
*   **Data Persistence:** Data is stored in `sessionStorage` with an expiry to prevent loss on page refresh.
*   **Modular Component Usage:** Renders several child components like `AirSegments`, `AirTickets`, and `Passengers` to display specific sections of the folder review.

**Patterns/Recurring Elements:**

*   **Heavy React Hook Usage:** Consistent and extensive use of `useState`, `useEffect`, `useRef`, and `react-router-dom` hooks (`useLocation`, `useParams`, `useNavigate`) for managing component lifecycle, state, and routing.
*   **Conditional Rendering:** Many UI elements and data fetches are conditionally rendered or executed based on component state or data availability (e.g., `if (!pnrInput)`, `if (propsData)`).
*   **Integration with External Libraries/Services:** Relies on `supabase` for backend interactions, `dayjs` for date manipulation, `flowbite-react` for UI components, and `primereact/toast` for notifications.
*   **Constant-Based Configuration:** Utilizes various constants (e.g., `bookingStatuses`, `priceFormattor`, `routeName`, `services`) to define application-wide settings and mappings, enhancing maintainability and readability.