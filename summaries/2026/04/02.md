# Activity Summary for 4/2/2026

## 1:17:14 PM
The provided log details changes to two files: `g:\wamp64\www\sitenettech\api-client\src\pages\backend\Search\CartPage.jsx` and `g:\wamp64\www\sitenettech\api-client\src\constants\constants.js`.

### File: `g:\wamp64\www\sitenettech\api-client\src\pages\backend\Search\CartPage.jsx`
**Timestamps:** 4/2/2026, 12:31:19 PM to 4/2/2026, 12:54:54 PM

This file, which appears to be a React component for a shopping cart page, shows a series of rapid updates.
The changes consistently involve:
*   **Imports:** A long list of imports remains stable across all entries, including React hooks, Redux components, Flowbite UI, React icons, Yup for validation, Supabase for data integration, various custom components (AirSegments, Accordion, ModalCustom, ConfirmModalBody, Button, SeatMap), constants, cart-related features (cartSlice, useAuth, useCartTimer), data fetching utilities (fetchCountries, axios), and flight-specific Redux actions (fetchAirlines, fetchAirports, setMinMaxPrice, setStops) and helper functions.
*   **State Management:** Consistent use of `useState` for UI states (e.g., `isOpen`, `removeModalOpen`, `isConfirmed`), price calculations (`totalCartPrice`, `flightTotal`, `flightTotalPassengar`), and data (`companyData`, `countriesOption`). Redux `useSelector` is used to retrieve cart data (`events`, `expired`, `cartTotalItems`, `basketId`, `cartTotalPrice`).
*   **Data Fetching:** The `fetchCompanyData` asynchronous function to fetch `price_rules` from Supabase's `companies` table based on `user.company_id` is present in all versions. `fetchCountries` is also consistently fetched on mount.
*   **Calculations & Logic:** Functions like `calculateFlightBreakdown` (for passenger-specific price details and commissions) and `clculateFlightTotal` (for overall flight cost considering company-specific pricing rules like `flight_fixed` and `flight_percentage`) are present and seem to be critical for the cart's pricing display.
*   **Form Handling & Validation:** The component uses `formik` for form management and `Yup` for validation.
    *   `buildInitialPassengers` creates the initial structure for adult, child, and infant passenger data based on flight details.
    *   `initialValues` for the formik form include `contact` information, `passengers`, and `payment` preferences.
    *   `passengerSchema` defines validation rules for passenger details (title, first name, last name, DOB, nationality) and passport information (country, number, issue/expiry dates).
*   **Validation Logic in `handleSubmit`:**
    *   **12:44:42 PM:** An `if (formik.isValid)` block was added to check overall form validity and log messages ("✅ All fields valid" or "❌ কিছু field invalid আছে" - "some fields are invalid").
    *   **12:46:35 PM:** A redundant `console.log("✅ All fields valid");` was added before the `if (formik.isValid)` block.
    *   **12:47:20 PM:** The `handleSubmit` function was updated to be `async` and explicitly `await formik.validateForm()`. It then checks `Object.keys(errors).length === 0` to determine validity and logs more detailed messages including validation errors. It also contains logic for different payment flows (`bookOnly`, `payWithCredit`).
    *   **12:54:54 PM:** Significant additions to `handleSubmit` were made. It now stores various contact and primary passenger details (`passenger_title`, `passenger_firstName`, `passenger_lastName`, `passenger_phone`, `passenger_email`, `passenger_address`, `passenger_city`, `passenger_zip`, `passenger_country`, `basket_total_cost`, `passenger_first_name`, `passenger_last_name`, `passenger_dob`) into `sessionStorage`. It also initializes a `paymentMethod` variable to "CARD".

**Pattern:** The changes to `CartPage.jsx` primarily focus on enhancing the form submission logic and validation, especially around the `handleSubmit` function, and then storing relevant form data and cart totals into `sessionStorage`. There's also a recurring pattern of `console.log` statements for debugging purposes.

### File: `g:\wamp64\www\sitenettech\api-client\src\constants\constants.js`
**Timestamp:** 4/2/2026, 12:57:08 PM

This file defines various constants used throughout the application. The provided log entry shows a single state of the file, suggesting a complete view rather than incremental changes. Key constants include:
*   `notifyTypes`, `namePrefixes`, `gender`, `Currency`.
*   `userMenus` with paths and slugs for navigation.
*   `HTTP_STATES` and `ERROR_STATUS` for API request handling and error management.
*   `ERROR_MESSAGE` for user-facing error strings.
*   `paymentMethods` (an array of objects) and `cardPaymentMethods` (an object defining values like "CARD", "BOOKONLY", "CREDIT").
*   `cabinClasses`, `journeyTypes`, `flightSuppliers`.
*   `priceFormattor` utility function.
*   `bookingStatuses` with `class` and `label` for different booking states.
*   `currency` (old structure) and `currency_symbol_list` (updated with USD, EUR, BDT, GBP symbols).
*   `formatTime` utility.
*   `colorPalette` object with an extensive list of Tailwind-like color definitions, including custom shades and specific brand colors like 'fiord', 'Slate Gray', 'Charcoal Gray', 'daisy-bush', 'olive-green', 'tapestry', 'mulled-wine', 'st-tropaz'.

**Pattern:** This file is a central repository for application-wide constants, ranging from UI configuration to API error handling and business logic values. It demonstrates a structured approach to managing static data.